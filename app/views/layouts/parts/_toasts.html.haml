%div{:style => "position: absolute; top: 2rem; right: 1rem;z-index:999999999"}
  / Then put toasts within
  - flash.each do |name, msg|
    .toast{class: "toast-#{name.to_s}","aria-atomic" => "true", "aria-live" => "assertive", :role => "alert",data: {autohide: 'false',controller: 'toast'}}
      .toast-header
        %strong.mr-auto
          -# = Settings.app_name
          communicator
        -# %small.text-muted just now
        %button.ms-auto.mb-1.btn-close{"aria-label" => "Close", "data-bs-dismiss" => "toast", :type => "button"}
      .toast-body
        = msg.html_safe

-# - flash.each do |key, value|
-#   .toast.show.align-items-center{atomic: true, "aria-live" => "assertive", :role => "alert"}
-#     .d-flex
-#       .toast-body
-#         = value
-#       %button.btn-close.me-2.m-auto{"aria-label" => "閉じる", "data-bs-dismiss" => "toast", :type => "button"}
