-# %h2 Change your password
-# = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f|
-#   = render "users/shared/error_messages", resource: resource
-#   = f.hidden_field :reset_password_token
-#   .field
-#     = f.label :password, "New password"
-#     %br/
-#     - if @minimum_password_length
-#       %em
-#         (#{@minimum_password_length} characters minimum)
-#       %br/
-#     = f.password_field :password, autofocus: true, autocomplete: "new-password"
-#   .field
-#     = f.label :password_confirmation, "Confirm new password"
-#     %br/
-#     = f.password_field :password_confirmation, autocomplete: "new-password"
-#   .actions
-#     = f.submit "Change my password"
-# = render "users/shared/links"


.form-container.mb-4
  = simple_form_for resource, as: resource_name, url: password_path(resource_name), html: { method: :put, class: 'form-center' }, data: {turbo: false} do |f|
    %h2.form-title.mb-4 パスワードリセット
    = f.input :password, autofocus: true, autocomplete: "new-password", required: false, input_html: { class: 'form-control stylish-input mb-2' }
    - if @minimum_password_length
      %small
        = "※#{@minimum_password_length}文字以上"
    = f.input :password_confirmation, autocomplete: "new-password", required: false, input_html: { class: 'form-control stylish-input mb-2' }
    = f.hidden_field :reset_password_token
    .actions.d-flex.align-items-end.justify-content-between
      = render "users/shared/links"
      = f.button :submit, 'パスワードを変更する', class: 'btn btn-primary btn-block mt-4 zozo-button'
      
