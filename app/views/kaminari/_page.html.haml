- if page.current?
  %li.page-item.active
    = content_tag :a, page, data: { remote: remote }, rel: page.rel, class: 'page-link', data: {turbo_stream: true}
- else
  %li.page-item
    = link_to page, url, remote: remote, rel: page.rel, class: 'page-link', data: {turbo_stream: true}
